# =============================================================================
# Stealth Addresses Utils - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values.
# NEVER commit .env files with real private keys to version control!
# =============================================================================

# =============================================================================
# META-ADDRESS
# =============================================================================
# The recipient's stealth meta-address for generating stealth addresses.
# Generate one with: pnpm create-sma
# Format: 0x + 134 hex characters (67 bytes)
#
META_ADDRESS=

# =============================================================================
# RECIPIENT PRIVATE KEYS (for key recovery)
# =============================================================================
# These are YOUR private keys if you are the recipient.
# Used to recover stealth private keys from announcements.
# Format: 0x + 64 hex characters (32 bytes)
#
VIEW_PRIVATE_KEY=
SPEND_PRIVATE_KEY=

# =============================================================================
# ANNOUNCEMENT DATA (for key recovery)
# =============================================================================
# Data from an announcement that was published by a sender.
# The ephemeral public key and view tag are needed to recover the stealth key.
#
# Ephemeral Public Key - compressed format
# Format: 0x + 66 hex characters (33 bytes, starts with 02 or 03)
EPHEMERAL_PUBLIC_KEY=

# View Tag - 1 byte
# Format: 0x + 2 hex characters
VIEW_TAG=

# =============================================================================
# PREDEFINED KEYS (optional, for create-sma script)
# =============================================================================
# If set, the create-sma script will use these keys instead of generating random ones.
# Useful for testing or when you want to use specific keys.
# Format: 0x + 64 hex characters (32 bytes)
#
# PREDEFINED_SPEND_PRIVATE_KEY=
# PREDEFINED_VIEW_PRIVATE_KEY=
